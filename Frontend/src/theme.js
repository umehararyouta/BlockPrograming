import { createTheme } from "@mui/material";

const theme = createTheme({

    palette:{
        background:{
            default:'#eef6fa'
        },
    },
    typography:{
        h6:{
            fontWeight:1000,
        }
        
    },
});

export default theme;